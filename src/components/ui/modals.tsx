'use client'

import { CreateRoomModal } from "@/features/rooms/components/create-room-modal";

type Props = {
 
}
export const Modals = ({}: Props) => {
    return ( 
        <>
            <CreateRoomModal/>
        </>
    );
}